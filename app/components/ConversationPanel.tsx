"use client";

import { ChangeEvent, FormEvent, useEffect, useMemo, useRef, useState } from "react";
import { api } from "../lib/api";
import { initials } from "../lib/format";
import type { AutomationIntent, Chat, CtaUrlMessage, DocumentOption, EntrepreneurPackage, Message, QuickReply, SavedSticker } from "../lib/types";
import { AudioRecorder } from "./AudioRecorder";
import { QuoteNoteGenerator } from "./QuoteNoteGenerator";
import { ChatSaleModal } from "./ChatSaleModal";
import { CtaUrlCard } from "./CtaUrlCard";
import { ToolAccordion } from "./ToolAccordion";
import { WhatsAppTemplateCard } from "./WhatsAppTemplateCard";

type Props = {
  chat: Chat | null; messages: Message[]; draft: string; uploadingAudio: boolean; uploadingMedia: boolean;
  documentOptions: DocumentOption[]; selectedDocumentId: string; documentCaption: string;
  onDraftChange: (value: string) => void; onSendText: (event: FormEvent) => void;
  replyToMessage: Message | null; onReplyToChange: (message: Message | null) => void;
  onUploadAudio: (event: ChangeEvent<HTMLInputElement>) => void; onUploadImage: (event: ChangeEvent<HTMLInputElement>) => void;
  onUploadVideo: (event: ChangeEvent<HTMLInputElement>) => void; onUploadDocument: (event: ChangeEvent<HTMLInputElement>) => void;
  onDocumentChange: (mediaId: string) => void; onDocumentCaptionChange: (caption: string) => void; onSendDocument: () => void;
  onSendCtaUrl: (data: CtaUrlMessage) => Promise<void>; entrepreneurPackages: EntrepreneurPackage[];
  quickReplies: QuickReply[]; stickers: SavedSticker[];
  onSendEntrepreneurPackages: (selection: { packageIds?: string[]; imageIds?: string[] }) => Promise<void>;
  onSendSticker: (id: string) => Promise<void>; onRecordAudio: (audio: Blob, filename: string) => Promise<void>;
  onAutoReplyChange: (enabled: boolean) => void; onScenarioChange: (enabled: boolean) => void;
  automationIntents: AutomationIntent[]; onLearnIntent: (messageId: string, intentId: string) => Promise<void>;
  onDeleteConversation: () => void; onClose?: () => void;
};

type ToolTab = "share" | "sale";

function IntentLearner({ message, intents, onLearn }: { message: Message; intents: AutomationIntent[]; onLearn: (messageId: string, intentId: string) => Promise<void> }) {
  const [intentId, setIntentId] = useState("");
  const [saving, setSaving] = useState(false);
  const [open, setOpen] = useState(false);
  if (message.direction !== "inbound" || message.type !== "text" || !message.body || !intents.length) return null;
  const learn = async () => { if (!intentId) return; setSaving(true); try { await onLearn(message.id, intentId); setOpen(false); setIntentId(""); } finally { setSaving(false); } };
  return <div className="intent-learner"><button className="intent-learner-trigger" type="button" aria-label="Entrenar bot" onClick={() => setOpen(value => !value)}>⋮</button>{open && <div className="intent-learner-menu"><select value={intentId} onChange={event => setIntentId(event.target.value)}><option value="">Este mensaje corresponde a…</option>{intents.map(intent => <option key={intent.id} value={intent.id}>{intent.name}</option>)}</select><button type="button" disabled={!intentId || saving} onClick={() => void learn()}>{saving ? "…" : "Aprender"}</button></div>}</div>;
}

export function ConversationPanel(props: Props) {
  const { chat, messages, draft, uploadingAudio, uploadingMedia, documentOptions, selectedDocumentId, documentCaption, onDraftChange, onSendText, replyToMessage, onReplyToChange, onUploadAudio, onUploadImage, onUploadVideo, onUploadDocument, onDocumentChange, onDocumentCaptionChange, onSendDocument, onSendCtaUrl, entrepreneurPackages, quickReplies, stickers, onSendEntrepreneurPackages, onSendSticker, onRecordAudio, onAutoReplyChange, onScenarioChange, automationIntents, onLearnIntent, onDeleteConversation, onClose } = props;
  const threadRef = useRef<HTMLDivElement>(null);
  const scrolled = useRef<string | null>(null);
  const [toolsOpen, setToolsOpen] = useState(false);
  const [toolsCollapsed, setToolsCollapsed] = useState(false);
  const [tab, setTab] = useState<ToolTab>("share");
  const [bundlePickerOpen, setBundlePickerOpen] = useState(false);
  const [saleModalOpen, setSaleModalOpen] = useState(false);
  const [chatMenuOpen, setChatMenuOpen] = useState(false);
  const [selectedImageIds, setSelectedImageIds] = useState<string[]>([]);
  const [selectedBundleIds, setSelectedBundleIds] = useState<string[]>([]);
  const [openImageSets, setOpenImageSets] = useState<string[]>([]);
  const [sending, setSending] = useState(false);
  const [notice, setNotice] = useState("");

  const quickRepliesMatch = draft.trim().startsWith("/") ? quickReplies.filter(reply => reply.shortcut.includes(draft.trim().toLowerCase()) || reply.name.toLowerCase().includes(draft.trim().slice(1).toLowerCase())).slice(0, 6) : [];
  const bundles = useMemo(() => entrepreneurPackages.filter(item => item.controlBundleId != null).reduce<Record<string, EntrepreneurPackage[]>>((all, item) => { const key = item.bundleType || "Sin categoría"; all[key] = [...(all[key] || []), item]; return all; }, {}), [entrepreneurPackages]);
  const images = useMemo(() => entrepreneurPackages.filter(item => item.controlBundleId == null).reduce<Record<string, EntrepreneurPackage[]>>((all, item) => { const key = item.imageCategory || "Sin categoría"; all[key] = [...(all[key] || []), item]; return all; }, {}), [entrepreneurPackages]);
  const imageCount = Object.values(images).flat().reduce((total, item) => total + item.images.length, 0);

  useEffect(() => { if (!chat?.id || !messages.length || scrolled.current === chat.id) return; const frame = requestAnimationFrame(() => { threadRef.current?.scrollTo({ top: threadRef.current.scrollHeight }); scrolled.current = chat.id; }); return () => cancelAnimationFrame(frame); }, [chat?.id, messages]);

  const toggleImage = (id: string) => setSelectedImageIds(current => current.includes(id) ? current.filter(value => value !== id) : [...current, id]);
  const toggleBundle = (id: string) => setSelectedBundleIds(current => current.includes(id) ? current.filter(value => value !== id) : [...current, id]);
  const toggleImageSet = (id: string) => setOpenImageSets(current => current.includes(id) ? current.filter(value => value !== id) : [...current, id]);
  const selectAllImages = (items: EntrepreneurPackage["images"]) => setSelectedImageIds(current => [...new Set([...current, ...items.map(image => image.id)])]);
  const sendSelectedImages = async () => { if (!selectedImageIds.length) return; setSending(true); setNotice(""); try { await onSendEntrepreneurPackages({ imageIds: selectedImageIds }); setSelectedImageIds([]); } catch (error) { setNotice(error instanceof Error ? error.message : "No fue posible enviar las imágenes."); } finally { setSending(false); } };
  const sendPackages = async () => { if (!selectedBundleIds.length) return; setSending(true); setNotice(""); try { await onSendEntrepreneurPackages({ packageIds: selectedBundleIds }); setSelectedBundleIds([]); setBundlePickerOpen(false); } catch (error) { setNotice(error instanceof Error ? error.message : "No fue posible enviar los paquetes."); } finally { setSending(false); } };
  const mediaUrl = (message: Message) => `${api}/conversations/${chat?.id}/messages/${message.id}/media`;
  const content = (message: Message) => {
    if (message.type === "audio" && message.media_id) return <audio controls src={mediaUrl(message)} />;
    if (message.type === "sticker" && message.media_id) return <img className="sticker" src={mediaUrl(message)} alt="Sticker" />;
    if (message.type === "image" && message.media_id) return <img className="chat-image" src={mediaUrl(message)} alt="Imagen" />;
    if (message.type === "video" && message.media_id) return <video className="chat-video" controls src={mediaUrl(message)} />;
    if (message.type === "document" && message.media_id) return <a className="document-card" href={mediaUrl(message)} target="_blank" rel="noreferrer"><b className="document-icon">PDF</b><span><strong>{message.filename || "Documento adjunto"}</strong><small>Abrir documento</small></span></a>;
    if (message.type === "interactive") { try { const interactive = JSON.parse(message.body || "{}") as CtaUrlMessage; return <div className="interactive-cta-preview"><p>{interactive.body || "Mensaje interactivo"}</p><span>{interactive.buttonText || "Abrir enlace"} ↗</span></div>; } catch { return <p>[Mensaje interactivo]</p>; } }
    if (message.type === "template") { try { const template = JSON.parse(message.body || "{}") as { templateName?: string; components?: { parameters?: { text?: string }[] }[] }; return <div className="interactive-cta-preview"><p>Plantilla: {template.templateName || "WhatsApp"}</p><span>{template.components?.flatMap(component => component.parameters || []).map(parameter => parameter.text).filter(Boolean).join(" · ") || "Enviada mediante plantilla oficial"}</span></div>; } catch { return <p>[Plantilla oficial]</p>; } }
    return <p>{message.body || `[${message.type}]`}</p>;
  };
  const fileTools = <div className="tool-file-cards"><label><b>PDF</b><small>Documento</small><input type="file" accept="application/pdf,.pdf" onChange={onUploadDocument} disabled={!chat || uploadingMedia} /></label><label><b>▧</b><small>Imagen</small><input type="file" accept="image/jpeg,image/png,image/webp" onChange={onUploadImage} disabled={!chat || uploadingMedia} /></label><label><b>▶</b><small>Video</small><input type="file" accept="video/mp4,video/3gpp" onChange={onUploadVideo} disabled={!chat || uploadingMedia} /></label></div>;

  const shareTools = <>
    <ToolAccordion defaultOpen icon="▤" title="Catálogo" description="Comparte un catálogo con un mensaje personalizado."><select value={selectedDocumentId} onChange={event => onDocumentChange(event.target.value)}><option value="">Selecciona un catálogo</option>{documentOptions.map(document => <option key={document.mediaId} value={document.mediaId}>{document.filename}</option>)}</select><textarea value={documentCaption} onChange={event => onDocumentCaptionChange(event.target.value)} placeholder="Mensaje que acompaña el catálogo" disabled={!selectedDocumentId} /><button className="tool-primary-button" type="button" disabled={!chat || !selectedDocumentId || uploadingMedia} onClick={onSendDocument}>Enviar catálogo</button></ToolAccordion>
    <CtaUrlCard disabled={!chat} packages={entrepreneurPackages} onSend={onSendCtaUrl} />
    <WhatsAppTemplateCard conversationId={chat?.id} disabled={!chat} />
    <ToolAccordion icon="＋" title="Adjuntar archivos" description="Comparte un archivo nuevo en este chat.">{fileTools}</ToolAccordion>
    <ToolAccordion icon="▦" title="Imágenes guardadas" description="Despliega un conjunto y elige solo las imágenes que quieres enviar.">{Object.entries(images).map(([category, sets]) => <div className="tool-gallery-category" key={category}><strong>{category}</strong>{sets.map(item => { const selectedCount = item.images.filter(image => selectedImageIds.includes(image.id)).length; const isOpen = openImageSets.includes(item.id); return <article className="saved-image-set" key={item.id}><button className="saved-image-set-trigger" type="button" onClick={() => toggleImageSet(item.id)} aria-expanded={isOpen}><span><b>{item.name}</b><small>{item.images.length} imágenes · {selectedCount} seleccionadas</small></span><span aria-hidden="true">{isOpen ? "⌃" : "⌄"}</span></button>{isOpen && <div className="saved-image-set-content"><button className="select-all-images" type="button" disabled={!item.images.length} onClick={() => selectAllImages(item.images)}>Seleccionar todas</button><div className="tool-gallery-grid">{item.images.map(image => <button key={image.id} type="button" className={selectedImageIds.includes(image.id) ? "selected" : ""} onClick={() => toggleImage(image.id)}><img src={`${api}/settings/entrepreneur-packages/images/${image.id}/media`} alt={image.filename || item.name} /><span>{image.filename || item.name}</span></button>)}</div></div>}</article>; })}</div>)}{!imageCount && <p className="tool-empty">No hay imágenes guardadas.</p>}{imageCount > 0 && <button className="tool-primary-button" type="button" disabled={!selectedImageIds.length || sending} onClick={() => void sendSelectedImages()}>{sending ? "Enviando…" : `Enviar seleccionadas${selectedImageIds.length ? ` (${selectedImageIds.length})` : ""}`}</button>}</ToolAccordion>
    {stickers.length > 0 && <ToolAccordion icon="☺" title="Stickers" description="Comparte un sticker con el cliente."><div className="tool-stickers">{stickers.map(sticker => <button key={sticker.id} type="button" onClick={() => void onSendSticker(sticker.id)}><img src={`${api}/settings/stickers/${sticker.id}/media`} alt={sticker.name} /></button>)}</div></ToolAccordion>}
  </>;
  const saleTools = <>
    <ToolAccordion defaultOpen icon="✓" title="Registrar venta" description="Descuenta inventario y vincula la conversión al chat." className="sale-tool-card"><label>Forma de pago<select defaultValue="pending"><option value="pending">Por definir al registrar</option><option value="cash">Efectivo</option><option value="transfer">Transferencia</option><option value="card">Tarjeta</option></select></label><button className="tool-primary-button" type="button" disabled={!chat} onClick={() => setSaleModalOpen(true)}>Registrar orden</button></ToolAccordion>
    <ToolAccordion icon="🎁" title="Paquetes y promociones" description="Envía fotografías de un bundle."><button className="tool-secondary-button" type="button" disabled={!chat} onClick={() => setBundlePickerOpen(true)}>Enviar bundle</button></ToolAccordion>
    <ToolAccordion icon="🧾" title="Nota final" description="Genera un resumen con productos, paquetes y envío." className="quote-tool-card"><QuoteNoteGenerator disabled={!chat} onUseNote={value => onDraftChange(value)} /></ToolAccordion>
  </>;
  const tools = <aside className={`chat-tools-panel ${toolsCollapsed ? "collapsed" : ""}`}><header><div><p>HERRAMIENTAS</p><h2>Enviar y preparar</h2></div><button className="chat-tools-toggle" type="button" onClick={() => setToolsCollapsed(value => !value)} aria-label={toolsCollapsed ? "Desplegar herramientas" : "Ocultar herramientas"} title={toolsCollapsed ? "Desplegar herramientas" : "Ocultar herramientas"}>{toolsCollapsed ? "‹" : "›"}</button></header>{!toolsCollapsed && <><nav className="chat-tools-tabs"><button className={tab === "share" ? "selected" : ""} type="button" onClick={() => setTab("share")}>Compartir contenido</button><button className={tab === "sale" ? "selected" : ""} type="button" onClick={() => setTab("sale")}>Gestión de venta</button></nav>{notice && <p className="tool-notice">{notice}</p>}<div className="chat-tools-body">{tab === "share" ? shareTools : saleTools}</div></>}</aside>;

  return <><section className="panel compact-chat-panel"><header className="chat-header"><b className="avatar">{chat ? initials(chat.name || chat.phone_number) : "M"}</b><div className="chat-title"><h2>{chat ? chat.name || chat.phone_number : "Tu bandeja está lista"}</h2><small>{chat?.phone_number || "Selecciona una conversación"}</small></div>{chat && <div className="chat-statuses"><button className={`status-badge ${chat.autoReplyEnabled !== false ? "on" : ""}`} type="button" onClick={() => onAutoReplyChange(chat.autoReplyEnabled === false)}>{chat.autoReplyEnabled !== false ? "✓" : "○"} Bot</button><button className={`status-badge ${chat.scenarioEnabled !== false ? "on" : ""}`} type="button" onClick={() => onScenarioChange(chat.scenarioEnabled === false)}>{chat.scenarioEnabled !== false ? "✓" : "○"} Escenarios</button></div>}{chat && <div className="chat-overflow"><button className="delete-chat-button" type="button" onClick={() => setChatMenuOpen(value => !value)}>Borrar prueba</button>{chatMenuOpen && <div><button type="button" onClick={() => { setChatMenuOpen(false); onDeleteConversation(); }}>Confirmar borrar conversación</button></div>}</div>}{onClose && <button className="close-conversation" type="button" onClick={onClose}>×</button>}</header><div className="thread" ref={threadRef}>{!chat && <div className="empty"><b className="mark">M</b><h2>Atiende desde un solo lugar</h2><p>Crea una conversación o espera un mensaje entrante.</p></div>}{messages.map(message => <article className={message.direction} key={message.id}>{message.replyToMessageId && <div className="quoted-message"><b>{message.replyToDirection === "inbound" ? chat?.name || "Cliente" : "Tú"}</b><span>{message.replyToBody || `[${message.replyToType || "mensaje"}]`}</span></div>}{content(message)}<div className="message-meta"><small>{new Date(message.created_at).toLocaleTimeString("es-MX", { hour: "2-digit", minute: "2-digit" })}</small><button type="button" className="reply-button" onClick={() => onReplyToChange(message)}>↩</button></div><IntentLearner message={message} intents={automationIntents} onLearn={onLearnIntent} /></article>)}</div>{bundlePickerOpen && <div className="bundle-picker-backdrop" onMouseDown={() => setBundlePickerOpen(false)}><section className="bundle-picker-modal" onMouseDown={event => event.stopPropagation()}><header><div><p>FOTOGRAFÍAS DE BUNDLES</p><h2>Enviar bundle</h2></div><button type="button" onClick={() => setBundlePickerOpen(false)}>×</button></header>{Object.entries(bundles).map(([category, items]) => <div className="bundle-picker-groups" key={category}><section><h3>{category}</h3>{items.map(item => <label key={item.id} className={selectedBundleIds.includes(item.id) ? "selected" : ""}>{item.images[0] ? <img src={`${api}/settings/entrepreneur-packages/images/${item.images[0].id}/media`} alt="" /> : <b>▧</b>}<span><strong>{item.name}</strong><small>{item.images.length} fotografías</small></span><input type="checkbox" checked={selectedBundleIds.includes(item.id)} onChange={() => toggleBundle(item.id)} /></label>)}</section></div>)}<footer><button type="button" className="plain-button" onClick={() => setBundlePickerOpen(false)}>Cancelar</button><button type="button" disabled={!selectedBundleIds.length || sending} onClick={() => void sendPackages()}>{sending ? "Enviando…" : "Enviar fotografías"}</button></footer></section></div>}<form className="composer compact-composer" onSubmit={onSendText}>{replyToMessage && <div className="reply-preview"><div><b>Respondiendo a {replyToMessage.direction === "inbound" ? chat?.name || "cliente" : "ti"}</b><span>{replyToMessage.body || `[${replyToMessage.type}]`}</span></div><button type="button" onClick={() => onReplyToChange(null)}>×</button></div>}<div className="composer-tools"><button className="composer-tools-button" type="button" disabled={!chat} onClick={() => setToolsOpen(value => !value)}>＋</button>{toolsOpen && <div className="composer-tools-popover"><button type="button" onClick={() => { setTab("share"); setToolsOpen(false); }}>▤ Catálogo</button>{fileTools}<button type="button" onClick={() => { setTab("sale"); setToolsOpen(false); setBundlePickerOpen(true); }}>🎁 Bundles</button><button type="button" onClick={() => { setTab("sale"); setToolsOpen(false); }}>🧾 Nota final</button></div>}</div><textarea disabled={!chat} value={draft} onChange={event => onDraftChange(event.target.value)} onKeyDown={event => { if (event.key === "Enter" && !event.shiftKey && draft.trim()) { event.preventDefault(); event.currentTarget.form?.requestSubmit(); } }} placeholder={chat ? "Escribe un mensaje…" : "Selecciona una conversación"} />{quickRepliesMatch.length > 0 && <div className="quick-reply-suggestions">{quickRepliesMatch.map(reply => <button key={reply.id} type="button" onClick={() => onDraftChange(reply.body)}><code>{reply.shortcut}</code><span><b>{reply.name}</b><small>{reply.body}</small></span></button>)}</div>}<label className={uploadingAudio ? "audio-upload loading" : "audio-upload"}>{uploadingAudio ? "…" : "♫"}<input type="file" accept="audio/aac,audio/mp4,audio/mpeg,audio/amr,audio/ogg,audio/opus" onChange={onUploadAudio} disabled={!chat || uploadingAudio} /></label><button disabled={!chat || !draft.trim()} className="send">↑</button></form><AudioRecorder disabled={!chat || uploadingAudio} onSend={onRecordAudio} /></section>{!onClose && tools}{saleModalOpen && chat && <ChatSaleModal chat={chat} onClose={() => setSaleModalOpen(false)} onSaved={async () => undefined} />}</>;
}
